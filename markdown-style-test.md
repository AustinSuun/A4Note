---
tags:
  - markdown
  - style-test
author:
  - A4 Note
draft: false
date: 2026-08-29
summary: 用于检查 Markdown 编辑、阅读和目录样式的测试文档
---

# Markdown 样式测试

这是一份用于检查 A4 Note Markdown 渲染效果的示例文档。可以分别切换“编辑”和“阅读”模式，并打开右上角的目录。

## 1. 标题层级

### 1.1 三级标题

#### 1.1.1 四级标题

##### 五级标题

###### 六级标题

## 2. 文本样式

普通文本、**粗体**、*斜体*、***粗斜体***、~~删除线~~、<u>下划线</u>、`行内代码`、==高亮文本==。

### 2.1 下划线

<u>中文下划线</u> · <u>Underline 123</u> · 普通文字与<u>下划线片段</u>混排。

使用 `<u>文字</u>` 表示下划线；`_文字_` 是斜体。请分别在编辑、阅读模式下检查，行内代码中的标签应保持原样。

这是一个[外部链接](https://example.com)，这是一个自动链接：https://example.com。

## 3. 列表与任务

- 无序列表项目
- 第二个项目
  - 嵌套项目
  - 另一个嵌套项目
- 最后一个项目

1. 有序列表第一项
2. 有序列表第二项
   1. 有序嵌套项目
   2. 另一个有序嵌套项目

- [x] 已完成任务
- [ ] 未完成任务
- [ ] 继续检查阅读模式

## 4. 引用与标注块

> 这是普通引用。
>
> 引用可以包含多行内容，用于检查左侧引用线和文字间距。

> [!NOTE] 设计说明
> 这是 Note 标注块，适合放置补充说明。

> [!TIP] 使用技巧
> 这是 Tip 标注块，可以放置快捷操作或建议。

> [!IMPORTANT] 重要信息
> 这是 Important 标注块，用于强调需要优先注意的内容。

> [!WARNING] 风险提示
> 这是 Warning 标注块，用于提醒潜在风险。

> [!CAUTION] 谨慎操作
> 这是 Caution 标注块，用于需要谨慎处理的操作。

> [!SUCCESS] 已完成
> 这是 Success 标注块，用于表示完成或成功状态。

> [!QUESTION] 常见问题
> 这是 Question 标注块，可以放置问题和回答。

> [!QUOTE] 经典引用
> 这是 Quote 标注块，用于突出一段引用内容。

## 5. 代码

行内代码示例：`const answer = 42;`

```ts
type Note = {
  title: string;
  tags: string[];
};

const note: Note = {
  title: 'Markdown 样式测试',
  tags: ['markdown', 'demo'],
};
```

```bash
npm run build
npm run verify
```

```text
纯文本代码块也应该保持等宽字体，并支持右上角复制按钮。
```

## 6. 表格

| 样式 | Markdown 语法 | 预期用途 |
| --- | --- | --- |
| 标题 | `## 标题` | 形成目录层级 |
| 标注 | `> [!NOTE] 内容` | 突出提示信息 |
| 任务 | `- [ ] 任务` | 跟踪待办事项 |
| 代码 | ```` ```ts ```` | 展示源码 |

## 7. 图片

下面的图片使用与本笔记同目录的配套 PNG，检查本地相对路径、大小限制和加载效果。复制本笔记到其他文件夹时，请同时复制 `markdown-style-test-image.png`。

![A4 Note 图标](./markdown-style-test-image.png "本地图片示例")

## 8. 分隔线与脚注

---

分隔线后的内容仍然属于当前章节。这里有一个脚注[^demo]。

[^demo]: 这是脚注内容，用于检查脚注标记和脚注列表的显示效果。

## 9. 数学公式

行内公式：$E = mc^2$。

块级公式：

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

## 10. 长文本与换行

这是一段较长的文本，用于检查阅读模式的行宽、段落间距和换行行为。在窗口宽度变化时，文字应该保持在内容区域内，不应该遮挡目录或产生横向滚动条。调整界面字体大小后，标题、正文、列表和标注块也应该保持清晰的层级关系。

最后再放一段连续文本，确认文档滚动区域、目录定位和标题高亮可以正常工作。
