import assert from 'node:assert/strict';
import { hintKeycaps } from '../src/shared/shortcuts/hintKeycaps.ts';
import { layoutShortcutHints, hintRectsOverlap } from '../src/shared/shortcuts/hintLayout.ts';
const checks=[];const check=(v,label)=>{assert.ok(v,label);checks.push(label);};
for(const [width,height] of [[800,600],[1280,800],[360,500]]) {
  const bounds={left:8,top:8,right:width-8,bottom:height-8};
  const tools=Array.from({length:9},(_,i)=>({id:`tool-${i}`,width:46,height:20,anchor:{left:(width-306)/2+i*34,right:(width-306)/2+i*34+30,top:height-78,bottom:height-48}}));
  const zoom=Array.from({length:3},(_,i)=>({id:`zoom-${i}`,width:50,height:20,anchor:{left:width-120+i*34,right:width-90+i*34,top:10,bottom:40}}));
  const items=[...tools,...zoom,...Array.from({length:16},(_,i)=>({id:`other-${i}`,width:82,height:20}))];
  const controls=[...tools,...zoom].map(x=>x.anchor).concat({left:0,top:height-26,right:width,bottom:height});
  const positions=layoutShortcutHints(items,bounds,controls);
  check(Object.keys(positions).length===items.length,`${width}: every shortcut fits`);
  const boxes=items.map(item=>{const p=positions[item.id];return {left:p.left,top:p.top,right:p.left+item.width,bottom:p.top+item.height};});
  check(boxes.every(b=>b.left>=bounds.left&&b.top>=bounds.top&&b.right<=bounds.right&&b.bottom<=bounds.bottom),`${width}: viewport bounds`);
  check(boxes.every(b=>controls.every(c=>!hintRectsOverlap(b,c))),`${width}: never cover a control or DEV strip`);
  check(boxes.every((b,i)=>boxes.slice(i+1).every(c=>!hintRectsOverlap(b,c))),`${width}: badges never overlap`);
  check([...tools,...zoom].every(item=>positions[item.id].placement==='adjacent'),`${width}: dense tools and right-side zoom retain anchors`);
}
const control={left:100,top:700,right:130,bottom:730};
check(hintRectsOverlap(control,{left:100,top:control.bottom-30,right:148,bottom:control.bottom-10}),'regression fixture catches old bottom-minus-30 icon overlap');
// Large sidebar tiles: use glyph/label obstacles instead of blocking all of a
// tile's empty padding. A key must remain within 12px of its own icon.
const tiles=Array.from({length:4},(_,i)=>({id:`scene-${i}`,beside:true,width:42,height:20,anchor:{left:55+(i%2)*140,top:100+Math.floor(i/2)*80,right:79+(i%2)*140,bottom:124+Math.floor(i/2)*80}}));
const tileObstacles=tiles.flatMap(t=>[t.anchor,{left:t.anchor.left-8,right:t.anchor.right+8,top:t.anchor.bottom+8,bottom:t.anchor.bottom+24}]);
const tilePositions=layoutShortcutHints(tiles,{left:8,top:80,right:290,bottom:280},tileObstacles);
check(tiles.every(t=>tilePositions[t.id]?.placement==='adjacent'),'sidebar tile icons retain adjacent keys');
check(tiles.every(t=>Math.min(Math.abs(tilePositions[t.id].left-t.anchor.right),Math.abs(tilePositions[t.id].top+20-t.anchor.top))<=12),'sidebar hints do not drift multiple tile rows');
const floatingItems=Array.from({length:10},(_,i)=>({id:`named-${i}`,width:60,height:21,floatingWidth:232,floatingHeight:21,group:i<5?'Reader':'Workbench'}));
const named=layoutShortcutHints(floatingItems,{left:8,top:8,right:792,bottom:592},[]);
check(floatingItems.every(t=>named[t.id]),'all named rows fit desktop viewport');
check(floatingItems.every((t,i)=>i===0||named[t.id].top>named[floatingItems[i-1].id].top),'named rows preserve registry order, no gap backfilling');
check(new Set(Object.values(named).map(p=>p.left)).size===1,'named rows align their left edge');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'+',ctrl:true,shift:true}))===JSON.stringify(['Ctrl','Shift','+']),'Plus key preserved as a real keycap, not split as a separator');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'q',code:'KeyA',semantics:'code',ctrl:true}))===JSON.stringify(['Ctrl','A']),'physical key hint uses effective code');
check(hintKeycaps({type:'keyboard',key:'enter',alt:true,ctrl:true}).at(-1)==='Enter','named keys use readable casing');
check(hintKeycaps({type:'mouse',button:4})[0]==='M5','side button keycap');
const twoGroups=floatingItems.slice(0,6).map((item,i)=>({...item,group:i<3?'A':'B'}));
const wrapped=layoutShortcutHints(twoGroups,{left:8,top:8,right:536,bottom:200},[]);
check(new Set(twoGroups.slice(3).map(t=>wrapped[t.id]?.left)).size===1&&wrapped['named-3'].left<wrapped['named-0'].left,'related actions wrap as a complete group');
const blocked=layoutShortcutHints([floatingItems[0]],{left:8,top:8,right:792,bottom:592},[{left:690,top:70,right:740,bottom:100}]);
check(blocked['named-0'].top>=104,'collision check includes the action label, not just keys');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'h',ctrl:true},true))==='["H"]','button keycap omits held Ctrl');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'n',ctrl:true,alt:true},true))==='["Alt","N"]','button keycap keeps required Alt modifier');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'+',ctrl:true,shift:true},true))==='["Shift","+"]','compact Plus binding preserves Shift and Plus');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'q',code:'KeyA',semantics:'code',ctrl:true,meta:true},true))==='["Meta","A"]','compact physical key keeps Meta');
check(JSON.stringify(hintKeycaps({type:'mouse',button:3},true))==='["M4"]','compact back side key M4 keeps button 3 mapping');
check(JSON.stringify(hintKeycaps({type:'keyboard',key:'h',ctrl:true}))==='["Ctrl","H"]','full display remains the default for settings and floating hints');
const chord={id:'chord',width:139,height:29,anchor:{left:400,right:432,top:700,bottom:732}};
const nextKey={id:'next',width:35,height:29,anchor:{left:437,right:469,top:700,bottom:732}};
const popup={left:350,right:720,top:100,bottom:692};
const underPopup=layoutShortcutHints([chord,nextKey],{left:8,top:8,right:992,bottom:790},[chord.anchor,nextKey.anchor,popup]);
check(underPopup.chord?.placement==='adjacent'&&underPopup.next?.placement==='adjacent','wide dock chord and side-key both fit below popup');
check(underPopup.chord.left+chord.width<chord.anchor.right,'trailing action key alignment leaves room for next button');
console.log(`Shortcut hint layout verification passed: ${checks.length} checks`);
