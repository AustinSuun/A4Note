# 合并后集成冒烟：阅读器标注链路（Arena，2026-09-21 22:0x）

- 对象：本地 `main a6b5d22`（含今日合并：F1 旋转页、F2 重叠高亮、F3 跨页、F4 Escape、F6 无文字层提示、35496a06 选区色带、fb5e3f2f 标注图层、24ea34e5 笔记工作台、青岩橡皮擦精度、origin/main #14）。
- 方式：分离检出 `.worktrees/integration-check-arena`，静态 debug 构建 `arena-smoke`（独立身份/HOME/AppData/WebView2 profile/隔离 SQLite），驱动 `.tmp/smoke/smoke-driver.mjs`（CDP 真实鼠标 + `node:sqlite` 只读）；合成 PDF：两页文字 + 一页无文字层。只读、不触碰正式资料库，不安装/发布/重启服务。
- 同一构建另跑：F1 旋转页驱动 62/63（唯一失败为已知位图占位探针抖动）、F3 跨页驱动 22/22、真实论文选区色带驱动（见 `docs/mcp-reader-selection-band-highlight-arena-2026-09-21.md` 集成复核一节）。

## 结果（20 项通过 / 1 项未通过）

| 项 | 结果 | 备注 |
| --- | --- | --- |
| 无文字层页面选高亮工具显示提示；拖动不产生记录且再次提示（F6） | 通过 | 提示 5 s 自动隐藏，驱动即时捕获 |
| 图形拖动中 Escape 取消草稿，无记录无 draft 标记；正常拖动生成 1 条 rect（F4） | 通过 | |
| 在已有高亮上再次拖选可生成第二条高亮；下划线可叠加在高亮行；同一绘制层 2 个 rect（F2） | 通过 | |
| 自由画笔 7 点笔迹；橡皮从中间擦过 → 同一记录变为 9 点含 1 个断点（拆分而非整段删除）；指针离开页面时橡皮预览隐藏 | 通过 | 青岩橡皮精度修复 |
| 标注图层入口（工具坞末尾）打开 radiogroup 选择器；「新建学习记录」后页 1 标记 5→0、SVG rect 2→0，数据库仍 5 行；点「显示图层「默认图层」」恢复 5 标记/2 rect（fb5e3f2f） | 通过 | |
| 「笔记工作台」入口打开后编辑器出现、PDF 页数不变、页 1 canvas 为同一实例（24ea34e5） | 通过 | |
| reload 后 5 条记录全部保留 | 通过 | 活动学习记录为空视图，属设计 |
| 标注列表面板用户入口（F5，0bd39f48） | 未通过 | 该卡青岩仍在进行中，尚未合入 main，预期缺失 |

## 观察

- 未发现今日各分支并行合并带来的相互回归；选区色带与图层过滤、笔记工作台与 PDF 渲染共存正常。
- 测试脚本经验：`Runtime.evaluate` 返回 DOM 节点会报「Object reference chain is too long」，应返回布尔/纯数据；无文字层页面的提示会自动隐藏，检查需即时。
