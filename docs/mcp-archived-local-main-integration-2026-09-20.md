# 归档成果本地集成报告

日期：2026-09-20（Asia/Shanghai）

## 范围与结论

用户授权：合并到本地 main，不推送、不安装、不打包发布。

本轮仅集成能够从共享脏工作树中明确分离的归档成果，不声称 17 项归档任务的全部代码均已合并。对话中说明 PDF 归档成果混有 e220fdf6 未归档修改并询问扩大范围，用户未选择扩大范围，仅要求继续，因此采用保守的部分集成方案。

- 源工作区：`D:/WorkSpace/Aster`，原分支 `feat/capture-folder-tree-0.6.2`，原 HEAD `4ac8cb7449bbc82b2137f68a50738587343dda8f`。
- 集成工作区：`D:/WorkSpace/Aster-wt/archived-20260920`。
- 集成分支：`integrate/archived-20260920`。
- 基准：GitHub main `8f59e66bde01c44ae549335a81073f9d8291ea11`。
- 源码只读快照：源工作区 `.tmp/archived-merge-20260920/snapshot/`，526 个允许范围内的源码/配置/测试文件；SHA-256 清单 `.tmp/archived-merge-20260920/manifest.json`。不含用户笔记、数据库、会话或凭据。
- 只使用显式文件清单暂存，不使用 `git add .`、reset、clean 或全量 stash。原工作区源码、索引及分支不切换、不覆盖；配对状态和报告单独更新。
- 本地现有依赖离线安装到独立 worktree；Cargo 输出独立。没有复用其他 Agent 的 node_modules、测试资料或用户窗口。

## 已集成或已覆盖的归档范围

| 任务 | 本轮处理 |
| --- | --- |
| 1a36f4ab | 标注菜单可见后初始焦点修复、工具名接线和浏览器回归；未一并复制高亮设置或其他菜单重设计。 |
| 5b56fd94 | Markdown 目录跟随、定位及文件类型标签相关实现和回归。 |
| 01f78963 | 保留 main 已有工具栏自适应；接入归档浮栏尺寸、透明背景样式及回归。未重做安装版验收。 |
| 07005692 | 任务阶段模型、视图、项目级会话及主看板接线。 |
| ea69511d | 新任务服务/网关源码与启动依赖进入版本管理；本轮没有重启或重新迁移现用服务。 |
| 8bf49110 | 验收服务、API/CLI/MCP、面板及标准 ID 唯一性修复。 |
| 6def401c | 引用及 Callout 阅读对齐、标题剥离修复和专项回归。 |
| 4ce9a8e2 | 空行行高、标题额外留白修复；原生验收历史不冒称为本轮重新执行。 |
| 1c92370d | 保留后续“直接发布到队列”流程；不恢复已取消的方案审批门禁。 |
| 164017b2 | 看板字号实现随任务板依赖集成；不恢复旧 0.1.13 版本号，不重建安装包。 |
| 52e977f9 | 看板顶部紧凑布局及接入提示组件，保留后续已存在的侧栏/标题栏接线。 |
| e88a37b9 | 审核区、标题栏操作、证据分区与主看板接线。 |
| c06ccee7 | 已有大详情弹窗、图片浏览及对比实现。使用快照中的旧实现，不跟随在途 f376994b 的后续 UI 修改。 |
| 889243fc | 多项目侧栏、项目偏好及请求隔离接线。 |

以上涉及任务板基础服务、插件、平台启动和 Rust 命令等必要依赖，因此不是只复制几个 UI 文件。现有 main 的历史发布文档、工作流、依赖锁和版本号保留；产品版本仍为 0.1.9，这不是新发布版本声明。

## 明确暂缓的 3 项

| 任务 | 原因 |
| --- | --- |
| 2a6ec117 | 高亮新层与共享 PDF Overlay/Toolbar 接线相邻；为避免混入不完整的文本编辑组合，本轮不合并整套高亮改动。 |
| fdcf97df | 自动高度依赖尚未归档的文本编辑共享基线，不能可靠拆成纯归档版本。 |
| a14eca65 | 验收卡不新增产品代码，但关联的框内编辑实现与 e220fdf6 的取消、输入法、回调修复混合。 |

本轮未修改/合并源快照中的 `src/features/reader/pdf/` 实现，保留 main 原有 PDF 基础。上述 3 项仍需单独集成，不会因本报告变为完成。未合入 e220fdf6 的新增 IME/memo 修复、专项脚本及夹具；未替它或 f376994b、4de2cac5 更改任务状态。

源快照已保存，后续应由 PDF 负责人给出明确可合并提交，或用户明确授权一并集成未归档修复后再验证。不要在原脏工作区反向回滚这些修改。

## 验证

隔离 Windows worktree 执行：

- `npm ci --offline --no-audit --no-fund`：成功，独立安装 333 个包。
- PowerShell `npm run verify`：退出 0，`A4Note verification passed`。测试子进程清除 TASKS 环境配置，避免连接真实项目。
- Rust：198 passed、0 failed、5 ignored；忽略项没有计作通过。
- Callout：14 assertions 通过。
- 目录跟随：35 个映射/缩放/接线契约通过，不冒称浏览器 E2E。
- 大详情组件：27 assertions 通过。
- 阶段模型与组件：74 assertions 通过，隔离 TypeScript 诊断 0。
- 阶段视图与真实隔离服务集成：53 checks 通过。
- 多项目切换、隔离及保留磁盘数据：42 checks 通过。
- 真实菜单组件、独立无头 Chrome：339 passed、0 failed。
- 本轮未进行 Windows 安装版 GUI、真实资料、系统输入法或 OS 拖拽验收。

### 失败记录与范围内修正

1. 第一次启动完整验证时，本轮命令中的路径转义产生错误字符，已停止本轮自有命令并使用正斜杠路径重新执行；不把第一次尝试算作成功。
2. 多项目专项最初仍断言“积压/进行中/待验收”旧阶段名，且通过已被图标按钮替代的旧可见文字点击“打开项目文件夹”。修正为当前五阶段名称和按钮的真实 aria-label，再完整重跑 42 项通过。未删除项目隔离、未保存草稿和移除不删磁盘等断言，也未为迎合旧测试恢复废弃审批。
3. 清除一个新 CSS 文件末尾多余空行，保持差异检查通过。
4. 构建保留大 chunk、混合静态/动态导入和 Rust dead_code 等既有警告；不把有警告写成零警告。

### 证据位置

在集成工作区：

- `.tmp/verify-final.log`、`.tmp/verify-final.exit`
- `.tmp/supplementary-results.json`（保留首次多项目失败记录，不能单独当作最终结论）
- `.tmp/multi-project-gate.log`（最终 42 项结果）
- `.tmp/verify-reader-popover-focus-browser.log`
- `.tmp/verify-task-stage-integration.log`
- `.tmp/verify-callout-reading-alignment.log`
- `.tmp/verify-markdown-toc-follow.log`
- `.tmp/merge-allowlist.json`

## 上游基准取得方式

Windows 主机常规 GitHub fetch 曾连接重置。本轮经 GitHub API 核实提交与完整树，并取得该上游签名提交的原始 payload/signature，重组 commit 对象后 SHA-1 与 `8f59e66bde01c44ae549335a81073f9d8291ea11` 完全一致。所需 tree 与双亲对象已在本地历史中，连通性检查为 0 missing objects。导入的是内容寻址完全一致的既有上游对象，不是重新创建相似历史，不更改其作者、签名或内容，也没有推送远端。

## 合并边界

最终提交和本地 main 位置见本轮交付回执。原 main 引用保留备份分支；仅在完整验证通过后快进本地 main。GitHub main、标签、Release、现用服务、现用安装包以及原开发工作区不会随本地合并自动改变。
