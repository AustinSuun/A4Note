# 离线任务交还入口

任务：`41a28025-ca72-4214-bc28-02c152e43761`

## 实现

- 仅在任务进行中、存在负责人且最后心跳超过 120 秒时显示交还入口。
- 交还原因必填；明确说明负责人会清空、任务回到队列，同时保留任务、附件和历史。
- 调用既有 `release_stale` 动作并使用当前 revision；冲突、权限和状态错误由现有错误区展示。
- 成功后刷新详情和列表；交还入口与删除危险区分离，窄窗口允许换行。

## 验证

- `npm run build`：通过。
- `npm run test:agent-status`：通过。
- VS Code diagnostics（任务板目录 error/warning）：0。
- 服务测试启动后 Windows 进程异常退出，代码为 `-1073741819`，无测试输出；本次未改服务端。

## 未验证

未对真实任务库执行交还。真实离线卡片点击与截图需在集成后由用户验收。
