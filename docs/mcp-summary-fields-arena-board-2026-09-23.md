# 总览字段联动：执行计划与阶段进度（未交付）

任务 `7603aaaf-b130-4230-aba5-297722a57dc0`，spec 9，领取 revision 10。
分支 `fix/summary-note-fields-arena-board`；基线本地 main `f891c15da11890f6b081444a59abc7ada2fa3ba3`。

## 顺序与前置状态

图片任务 d3cdee12 已实现、完整整合验证、安全合并本地 main，并 submit 为 review/revision27。其结果不等于用户验收；本任务继承其图片导入、paperId 解析和表格缩略图，不另建资产实现。阅读器面板3932f561已到spec12/review，最新交付记录也在本基线，不覆盖其布局/动画改动。

用户授权逐项领取直到执行队列完成，再单独打包、推送和发布。当前仅领取本任务；新 queued 的可选资料库存储目录、文献库导航/列设置、页码、论文菜单等尚未领取。白板仅讨论。

## 已核实的参考和源码

已下载全部5张 reference，逐张SHA-256与任务元数据相符，已目视查看；文件在本 worktree `.tmp/reference/`。

- 旧笔记图确实暴露 `a4-summary` 注释。局部图“测试1”在 figure 的结束标记后，不能把表格空白直接判为同步故障，更不能自动归入结构或评估字段。
- 第三行“结构”单元格有明显未使用高度而只显示两行。源码 `summaryExcerpt` 的80/160/500字符截断及总览固定`--summary-lines:2`都需去除，改为实际容器空间测量。
- 被圈竖线的确切DOM/滚动容器及拖动中位置还未量测；目前不能断言是滚动条、旧位置残影或容器边界。将使用真实隔离窗口的滚动容器矩形、overflow/scrollHeight、命中元素和拖动帧确认，不只隐藏线条。
- 基线 `summaryNoteTemplate` 曾为所有非元数据列生成空字段及技术说明；本轮改为空字符串，仍校验目录，不批量改旧正文。
- `summaryFields` 已按稳定ID解析、忽略围栏并拒绝重复/嵌套/不配对标记；`updateSummaryField` 用偏移局部替换。新分区需要保留字段块外的全部原始区间和顺序，不能只把Map重新序列化成全文。
- `summaryNoteSession` 是同一个 NoteDocumentSession 的适配器，不是独立存储。分区、总览表格和完整总结编辑继续复用它及既有保存冲突/变更通知，不建第二套笔记来源。

## 已完成的基础与按需模板（正常分区UI尚未接入）

- 新增 `src/core/summaryFieldCatalog.ts`：显示名与稳定ID分离，纯函数新增/重命名/排序，保留其它列属性；校验空名、控制字符、80字符上限、规范化重名、ID重复/非法及既有24列上限。调用方提供新ID，不从名称派生。
- `scripts/verify-summary-field-catalog.mjs` 直接运行 **28项通过**，包含持久化格式往返保留ID/名称/显隐/列宽和行高；`tsc -b`及diff check通过。
- 目录现已接下述管理UI和既有配置保存。无损块解析器及有序字段/自由区模型保留原块及LF/CRLF；自由区修改、归类预览、明确追加/创建，以及paper+note身份/基线确认共60项检查通过。归类确认函数仅返回候选，不执行保存，不声称失败回滚已实现。
- 新/自动总结模板为空，已有笔记不改写；JS补齐回归和9项Rust补齐测试通过，含有字段A/空白B/旧标记C三论文存储保留。此C为测试夹具，非原生UI联动验收。
- `test:summary-fields` 已注册到统一verify。本轮 `npm run verify` 完整通过，Rust 231通过/0失败/5忽略，用时207837ms，日志 `.tmp/summary-model-verify.log`；包含28目录/60文档模型和原23 library-summary。build/tsc/architecture均通过。未执行Windows发行打包。
- 已启动独立原生实例 `arena-summary`，1489/CDP9289，身份 `app.aster.research.dev.arena-summary.wb8149a51b9`；不得使用生产资料库。源码候选：reader-writing-layout.css:245将滚动根 `.markdown-live-codemirror` 连同正文限制为720px；需实际DOM/拖动验证，尚未修CSS。

## 字段目录UI增量（本阶段，不等于整项交付）

- 总览新增“管理字段”原生dialog：自定义UUID稳定ID、重命名、显隐、上下排序及草稿取消；修改显示名不修改旧字段标题/正文。不覆盖另一个排队任务的通用列设置弹层。
- 使用既有summaryLayoutSession和期望内容保存；核对打开时目录，保留布局其他属性。失败仍显示错误并保留已暂存设置，禁止重试前改变候选，关闭按钮明确提示保留待重试设置。此机制仅用于目录；不能把它当作自由内容归类事务的实现。
- 新增真实浏览器dialog回归10项（模拟保存回调），已加入test:summary-fields和统一verify链：取消/焦点、重复名、稳定ID、显隐/顺序、失败保留及同候选重试、保存中Escape不关闭。
- 独立原生环境已实际保存并WebView刷新：figure仍为同一ID、显示名改为结构示意；自定义字段ID/隐藏/次序保留；A/B/C三篇noteId及完整正文逐字相同，B仍空白。最终记录`.tmp/shots/summary-fields-native/catalog-result.json`及`catalog-after-reload.png`，截图已目视检查。不是原生应用重启证明。
- 本增量build/tsc、28目录+60模型+10浏览器、architecture/diff通过；前述207837ms完整verify属于模型/模板提交90be4fe，不冒充本UI增量的完整verify。
- 原生测试初次被旧隐藏PDF层拦截；仅通过工作台closeTab关闭自有A测试标签后继续，没有隐藏CSS或force点击。第二次实际保存成功后，脚本未重新选择刷新后默认“列表”的“综览”而超时；最终reload脚本正确导航并完成上述3项检查。早期catalog-error.json不是最终结果。

## 完整总结分区编辑增量（先前阶段）

- `SummaryDocumentEditor` 是原会话的视图，不建第二份存储。在“编辑完整总结”窗口按原顺序显示字段/自由区，隐藏已解析映射，保留高级源码修复入口；只挂载当前区域的共享MarkdownLiveEditor，复用托管图片入口和Markdown预览。
- 支持选择目录字段并明确“添加到本篇”，以及在末尾写自由内容；仅选择或切换源码不补字段。没有单篇直接新建自定义目录入口，暂需先用管理字段；阅读器普通/总结侧栏仍保持原实现。
- `summaryEditorPatch` 将CodeMirror的LF文本差异映射回原始区间，不重写未变化的混合行尾；字段编辑拒绝注入/增删其他映射、缺少明确添加、元数据写入和过期正文。22项纯检查已注册test:summary-fields（总计28目录+60模型+22编辑补丁+10浏览器）。归类的选区映射与实际CAS事务仍未接入。
- 原生4项通过，`editor-result.json`/`editor-normal-saved.png`：初始空白B只加入figure并真实保存正文、末尾自由内容未进入figure；A/C全文不变；高级源码与保存内容一致；同一会话回显总览单元格。无pageerror。截图已目视；随后仅调整自由区编号及正常界面保存/路径文案，不改变保存流程。
- B夹具目前已因上述明确UI编辑变为一个字段＋自由内容；不要重跑假定B为空的seed/catalog-reload/editor初始化脚本，也不要自动重置笔记。`editor-before.json`保存编辑前基线。
- 原生脚本最初误用论文标题按钮打开阅读器，已改为本行“聚焦”；关闭自有B测试标签，没有force点击或隐藏层CSS。期间apply_patch两次权限拒绝；用户要求再试后同一工具恢复，未绕过权限。
- 当前增量build/tsc/architecture和120项检查通过；尚未重新跑最终完整verify、应用重启或双向/多主题验收，不代表整项任务完成。

## 阅读器同源分区与工具转接（本轮）

- 指定总结笔记的正常编辑和阅读模式现复用SummaryDocumentEditor；普通笔记分支未替换。继续使用固定paperId/noteId的NoteDocumentSession、原900ms自动保存/失焦/Ctrl+S/切换保护，不新建保存源。指定总结的外部追加不再trimEnd旧内容。
- forwardRef将格式/模板/选区/图片方法转给当前区域的共享编辑器；未选区域会提示，不猜字段；只读或非活动面板不接受这些写入。标注预览已接原跳转回调，实际标注专项仍需补验。高级源码切回阅读时仍呈现正常分区。
- 新增9项浏览器检查（真实共享编辑器，模拟目录IO/父状态），加入test:summary-fields。原有66项note-workbench通过；字段链现为28+60+22+10+9，共129项。
- 原生4项通过：阅读分区不改原文；在B指定总结字段中编辑，完整内容等于仅该字段的补丁，A/C全文不变；阅读器保存回显总览；总览原位编辑后回到同一noteId的阅读器分区。`reader-before.json`/`reader-result.json`、`reader-field-saved.png`/`reader-from-overview.png`；无pageerror，后一图已目视。仅默认窄窗悬浮状态，不冒充并排/写作/多主题/100–220%矩阵；浮动工具栏与滚动空间仍需最终布局验收。
- 本轮整套`npm run verify`退出0，用时244175ms，Rust 231通过/0失败/5忽略；`.tmp/summary-reader-verify.log`（UTF16）。覆盖本轮全部产品改动及已注册回归，不等于整项任务完成。
- 期间远端502/会话过期，重建MCP会话；发现原实例端口不可用后核验本方锁的PID19300已不存在且1489/9289均无人监听，只删除本方陈旧锁，使用相同隔离身份/资料库重启。未复制或操作生产库，没有结束他人会话。随后原生测试沿用已有B字段/自由内容，不重置夹具。
- 下一阶段归类必须在实际CAS成功后才发布移除结果，不能update再flush冒充事务。NoteDocumentSession和TextDocumentSession/总结适配器须共同处理：SummaryNoteSession的super writer为空实现，新事务方法必须显式委托真实note会话；还要保留保存期间并发草稿，防止后续自动flush抹掉归类结果。以上仅设计注意事项，尚未实现。

## 单篇字段目录入口与跨视图更新（本轮）

- 阅读器和完整总结共用“新建或管理字段”入口，直接复用SummaryFieldSettings，不另造字段表单/校验/标识规则。先保存全局定义，再明确“添加到本篇”；新增一个字段后只预选ID，保存目录不会改任何笔记。只读/非活动面板不显示入口。
- 抽出平台saveSummaryFieldCatalog复用原会话/CAS、基线冲突与失败重试路径，保留原布局扩展/行高/宽度；保存后仍检查会话是否有未落盘变化。不涉及尚未实现的正文归类事务。
- 总览订阅目录正文变化并刷新列/尺寸，解决从阅读器修改目录后已挂载总览不更新的问题。目录对话框阻止键盘事件冒泡到笔记编辑器，Ctrl+S仅保存目录，未确认重命名不能被快捷键绕过，Escape仅关闭内层并恢复触发器焦点。
- 新增11项真实组件浏览器检查（含同屏总览、真实会话与保存helper，模拟native CAS）：取消、重名、失败保留原文、稳定ID重试、布局扩展保留、快捷键、总览即时刷新、仅明确添加才改笔记、只读、过期提案拒绝、无pageerror。已注册test:summary-fields，总计140项。首次因select标签包含选项而精确定位失败，补显式可访问名称后通过，没有force点击。
- 原生5项通过：取消无写入；创建目录不写三篇笔记；明确添加并编辑只修改B同一noteId且旧正文/A/C保留；总览和完整总结回显、内层Escape不关外层；reload后目录ID、布局附加属性和笔记内容完全一致。`per-note-catalog-before.json`/`per-note-catalog-result.json`/`per-note-catalog-full.png`，截图已目视，无pageerror。B增加custom-2dc280fb-2670-4d5c-99ed-eeef275ec250（本篇入口验收字段）。
- 原生首轮完成前3项后遗漏“聚焦”之后的“编辑完整总结”按钮而超时；续测重新核对原始基线和当前存储后完成剩余2项，没有重复创建、重置或覆盖夹具。reload不等于进程重启，失败IO仅浏览器模拟，不冒充原生故障验收。
- 整套npm run verify退出0，用时208689ms；Rust231通过/0失败/5忽略。`.tmp/summary-per-note-verify.log`为UTF16。本轮不改归类/选区接口，不冒充完成整个spec9。

## 保存后发布的归类事务基础（本轮，UI尚未接入）

- NoteDocumentSession与TextDocumentSession新增commitContent(expected,next)：要求干净且匹配的基线，共用既有running队列和真实CAS writer，完成IO才发布候选正文。失败保留原文；普通自动保存仍走原update/flush。SummaryNoteSession显式委托真实note会话，不能落到super的空writer。
- 保存期间的新编辑保留为草稿；IO已成功但出现新编辑（含编辑后撤回到原值）时保留旧CAS基线并锁住后续自动保存/再次提交，要求导出和明确重新读取后合并。错误返回noteId不发布正文或跨笔记通知。重读/恢复/路径切换不能在新事务中途替换会话状态。
- 新增48项真实会话及适配器浏览器检查，IO受控模拟；覆盖延迟发布、同一flush队列、失败重试、并发/ABA、过期/未保存基线、只读式原文保留、错误ID、显式重读解锁、暂停、no-op、真实适配器委托和通知保护。已注册字段链，总计188项；note-workbench66通过。
- 隔离DEV原生API验证5项通过：纯计划不写盘；真实upsert_note版本冲突拒绝且保留原文和外部修改；延迟真实writer时界面会话/磁盘均未提前移除；释放后仅一次真实写入再发布准确计划；A/C全文不变、reload后内容一致。证据transaction-v3-before.json/transaction-v3-result.json，无pageerror。不是选区/确认UI验收，也不是进程重启或原生写入期间并发编辑的完整矩阵。
- 原生v1测试误试拦截只读的TAURI_INTERNALS.invoke，实际成功保存后等待钩子超时；v2独立import命中了不同模块实例，未找到已存在note会话。均为测试装置问题，没有改成可写或强行覆盖原生桥。v3从本方适配器取得并核验底层paperId/noteId，临时包裹其既有writer，最终恢复原writer；真实IPC未模拟。未重复原选区、回滚或重置夹具。
- B现已明确通过API计划将“测试自由 B”和“事务并发保留标记”追加到figure，保留原自由段其余字节、自定义字段及后续并发标记。每轮前基线分别保存在transaction[-v2/-v3]-before.json，不可重跑旧一次性脚本；A/C仍未改。
- 全套npm run verify退出0，用时207889ms，Rust231通过/0失败/5忽略；.tmp/summary-transaction-verify.log（UTF16）。产品只增加事务基础，选区快照/CRLF映射、预览/确认、UI取消及切换保护仍未接入；旧版总结逻辑来源切换的身份约束也需在接入时专项核对。

## 指定总结的显式选区归类UI（本轮）

- 共享Markdown编辑器新增单一真实选区快照（全文、UTF16起止与所选文字），拒绝空/多选区；summaryEditorSelection校验整个LF快照并映射CRLF/混合行尾原文偏移，拒绝半个代理对或过期范围。只在当前自由区明确选中时提供归类，不按标题或邻近字段推断。
- 指定总结的阅读器/完整总结接目标字段选择、所选原文与目标结果预览、明确追加/添加字段并归类或取消。已有目标只追加；不存在的目标要明确确认创建，取消不补字段。确认先flush当前草稿，再复核组件存活/活动状态、scope、正文和目录目标，才commitContent。成功转到目标编辑区，失败不提前移除原文，保存中禁重复确认。
- Dialog不将Escape/Ctrl+S冒泡到父笔记；正常完成的保存状态用snapshot.status而非队列pending值，避免finally清队列但未再次通知导致“正在保存”残留。原生截图发现目标下拉框高度裁字，改为字体相对高度并用实际文字行盒测量验证，不假定line-height一定是数字。
- 新增25项浏览器检查，实际SummaryEditor/共享CM/Dialog/会话，模拟目录和CAS IO；覆盖10项原始偏移/无效选区、无选区、精确预览、取消/缺失字段、失败保留、延迟发布、追加/明确新建、目标编辑区、保存文案、原文变更拒绝、预保存期间卸载不再发归类写入、旧来源关闭入口、几何及无pageerror。字段链现213项，note-workbench66通过。
- 原生UI主流程5项通过（assignment-ui-result.json）：阅读器精确选区预览/取消、既有字段真实追加；完整总结缺失字段取消/明确新建；reload持久化，A/C始终不变。另assignment-ui-geometry.json验证下拉框文字行盒与C“测试1”取消，三篇全文不变。修后主图assignment-ui-reader-preview-fixed.png已目视，C空结构仍为空，测试1仍自由；不把空结构误报同步失败。最终verify结束后补拍成功，assignment-ui-caption.json确认已保存文案与三篇全文不变，assignment-ui-full-saved-fixed.png已目视。
- B已通过本轮UI明确将“，不自动归类。”所在自由区追加到custom-2dc280fb-2670-4d5c-99ed-eeef275ec250，再将末尾并发标记自由区归入新建finding；不再假定B有原自由区。assignment-ui-before.json保留本轮基线，不重跑一次性脚本/重置笔记。
- 测试装置记录：浏览器初次误写Window.closed只读属性，改独立计数名；原生初次端口未开且无旧锁，启动同一隔离DEV；恢复的隐藏PDF拦截点击，仅正常关闭自有A/B/C测试标签，无force或遮盖CSS。首轮verify测量断言将浏览器normal行高当数值而失败，改实际行盒测量并修正截图发现的问题后重新完整验证。verify构建native-host资源会触发DEV自动重编，补拍连接短暂拒绝不是已证明的产品崩溃。
- 最终完整npm run verify退出0/217817ms，Rust231通过/0失败/5忽略，.tmp/summary-assignment-ui-final-verify.log（UTF16）。旧版独立MD的逻辑来源可能切换，尚未具备后端原子身份约束，明确关闭该来源归类入口并提示源码编辑；没有假称旧来源归类完成。原生UI故障/并发切换矩阵、图片标注/主题缩放及整体布局仍待验收。

## 后续实现阶段

1. **无损文档模型（完整总结与阅读器均已接入）**：显式字段块与前/间/后自由片段保留原位置；修改自由正文保持所有原字段块；异常标记停止结构化写入并可切高级源码。正文编辑已映射LF差异；归类仍需把编辑器选区映射到原文，不能直接复用归一化偏移。
2. **统一目录/按需新增（已接入）**：全局字段名、显隐、排序持久化；单篇可新建/管理目录并选择尚未加入的字段，明确添加才改本篇；新列不触发其他笔记补写。新建与自动补齐只产生简洁空笔记，保持已有笔记原样。
3. **字段区＋自由区**：阅读器和完整总结正常模式明确显示同源分区；仅当前编辑区挂载共享Markdown编辑器，继续支持托管图片与代码；高级源码明确切换。自由区保留原相对顺序，提示不写入正文。
4. **显式归类（指定总结已接UI，旧来源待补）**：选择自由文本→目标字段→预览→明确追加或取消；flush后复核scope/正文再commitContent。继续核验旧独立MD逻辑来源的原子身份约束、原生失败/并发切换与恢复入口，不用前端预检查冒充后端CAS身份保障。
5. **表格显示/滚动根因**：用真实可用高度、列宽、字体行高和UI缩放决定可见行数，只在实际溢出时省略并提供键盘全文入口；保留用户手动行高/列宽。复用图片任务的安全缩略图和放大器。对侧栏竖线先取DOM证据，再做最小根因修复。
6. **验证与交付**：A有字段、B无字段、C旧标记至少三篇；分别验证双向保存、空格取消、不跨论文/笔记、自由文字显式归类及失败、图片字段内外、刷新/真正重启。隔离dev:live拍真实前后图，100%/220%阅读缩放、窄窗、深浅主题、行高/列宽/虚拟滚动；TS/Rust/完整verify、双状态、本地main安全合并后submit。

## 风险与限制

- 模式切换、异步保存及表格虚拟卸载必须绑定paperId/noteId/字段ID和文档代次；不能用显示标题猜归属。
- 图片任务观察到隐藏PDF子层拦截其他场景鼠标，以及仅修改theme属性时的深色表格对比问题；这不是本任务已定位的竖线根因，不能混为一谈。正式主题验证走真实设置流程。
- 现有检查不等于整项验收。旧独立MD归类身份约束及完整事务交互/恢复矩阵、自适应摘要、侧栏根因、多主题缩放/图片/标注/冲突/完整重启验收及最终整合仍待完成。未合并main/submit，没有Windows打包、安装、推送、发布，也不自行验收归档。
